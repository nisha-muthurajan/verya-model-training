# Process Mining Input

Optional CSV and XES event logs provide observed workflow context. CSV input requires `case_id`, `activity`, and `timestamp` columns before it can be incorporated into analysis.
