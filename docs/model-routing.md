# Model Routing

Routing selects a model from the local catalog using task type, capability, complexity, cost, and risk tolerance. The decision records the chosen provider and the reasoning used for that selection.
