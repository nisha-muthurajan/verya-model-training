# Provider Boundaries

External providers are isolated behind small model-facing functions. Local fallbacks and structured error handling keep provider availability from changing the shape of pipeline reports.
