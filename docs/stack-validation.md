# Stack Validation

Stack validation checks the requested technologies against workflow needs. Inputs are normalized before comparison, and missing or incompatible choices are returned as structured issues.
