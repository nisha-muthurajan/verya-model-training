# Data Flow

A requirement becomes a workflow graph, then flows through the analysis models. Decisions retain task identifiers so risk, verification, and reputation stages can correlate their results with the original workflow.

Typed schema objects define the boundaries between stages.
