# Workflow Stage

Workflow analysis converts a plain-language requirement into tasks and dependencies. The resulting graph gives later stages a stable execution order and preserves relationships between tasks.
