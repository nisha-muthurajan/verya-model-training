# Flaw Detection

Flaw detection combines provider results with local security and consistency rules. Findings identify affected task IDs, severity, category, and a suggested correction.

Malformed provider responses are treated as provider failures rather than silently trusted data.
