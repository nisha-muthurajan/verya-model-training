# Architecture Overview

The project evaluates software requirements through a staged pipeline. Each stage receives typed data from the shared schema and returns a structured result for the next stage.

The main stages cover workflow extraction, flaw detection, stack validation, algorithm selection, model routing, risk prediction, verification, and reputation updates.
