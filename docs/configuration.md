# Configuration

Runtime configuration is loaded from environment variables and `.env` during local development. Provider credentials belong in the environment and should never be committed.

The checked-in `.env.example` documents supported variable names without containing secrets.
